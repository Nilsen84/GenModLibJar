package club.maxstats.modloader;

public class HookManager {
    /* Hook's a Minecraft Class for Injection
    * If you add a hook that's already previously added, it will overwrite the previous hook */
    public void addHook(Hook hookClass) {}
    public void addHooks(Hook... hooks) {}
}
